# Wyshbone Chat Agent

## Overview
The Wyshbone Chat Agent is an AI-powered assistant designed to enhance business research, facilitate contact finding, and streamline business discovery using the Wyshbone Global Database. It provides comprehensive analysis, lead generation, and business discovery, especially when user intent is ambiguous. The project aims to deliver a robust platform for sales and lead generation, capable of serving a broad market with its dual-mode AI architecture and integrated workflows.

## User Preferences
I want the agent to focus on practical, UK-focused responses. I want to ensure that any contact information discovered is public and verifiable, with no guessing of private details. I prefer a workflow that prioritizes Wyshbone Global Database as the authoritative source for business discovery. The agent should be able to intelligently decide when to search for new venues versus using cached information and support conversational queries without triggering unnecessary searches. I want the agent to auto-detect and execute Bubble batch workflows based on natural language commands. CRITICAL: The AI must ALWAYS ask for confirmation when making assumptions or combining current input with historical facts/context - chat history and facts serve as background reference, not primary drivers.
CRITICAL DATABASE RULE: ALL database connections MUST use `SUPABASE_DATABASE_URL` directly. NO fallback patterns—Replit's runtime auto-provides `DATABASE_URL` for its built-in Postgres, which conflicts with Supabase. Using `SUPABASE_DATABASE_URL` exclusively avoids this runtime conflict. The app crashes on startup if `SUPABASE_DATABASE_URL` is not set or empty.

## System Architecture
The application features a Node.js/Express backend and a React frontend, built with TypeScript, Tailwind CSS, and shadcn/ui, with TanStack Query managing API state. Core AI interactions utilize OpenAI's GPT models. The system supports multi-tenant user isolation with session-based authentication and robust data security.

**3-Way Chat Router (Trust-Hardened):**
- **CHAT_INFO:** GPT-5 streaming for informational/conversational responses. No execution language. Legacy SYSTEM_PROMPT from `memory.ts` is stripped via `omitLegacySystemPrompt: true`; only the CHAT_INFO prompt + user context + facts are sent to the LLM.
- **CLARIFY_FOR_RUN:** Entity-finding intent detected but missing info (location, entity type, or has semantic constraints). Creates an **in-memory** clarify session (no DB writes). Multi-turn: user replies are intercepted by session handler before the 3-way router runs. UI shows amber "Clarifying before run" badge with **structured `clarify_state`** payload from the server (entityType, location, semanticConstraint, count, missingFields, status, pendingQuestions). The client renders this structured data directly — NO regex parsing of markdown prose for the yellow box. When new data fills all required fields, presents a confirmation summary and waits for user to confirm (e.g. "search now", "yes", "go ahead") before transitioning to RUN_SUPERVISOR. Bare acknowledgements ("yes", "ok", "go ahead") also trigger transition when all fields are filled. Typed "search now" with missing fields returns a clear error message listing what's needed (not a silent fallthrough). The count regex in `parseAnswerContent` is context-aware: numbers followed by time units ("12 months", "3 years") are NOT extracted as result count.
- **RUN_SUPERVISOR:** Entity-finding intent with complete parameters. Creates supervisor task for execution.
- **Follow-Up Reuse:** After a successful supervisor task creation, `saveLastSuccessfulIntent()` stores the intent (business_type, location, count, attributes) in-memory per conversation with 60-min TTL. On subsequent messages, a FOLLOWUP_REUSE check runs AFTER the CLARIFY_GUARD but BEFORE the 3-way router. `detectFollowupReuse()` detects patterns like "now do York", "same thing but Leeds", "how about Bristol", "in Sheffield". If detected and no scope change (new entity type), constructs a new intent by reusing prior constraints and replacing only the location, then routes directly to RUN_SUPERVISOR. Scope changes ("find breweries in York") are correctly detected and fall through to the normal router. Empty intents (missing business_type or location) are silently rejected by `saveLastSuccessfulIntent`. Storage: in-memory `lastIntents` Map, lost on server restart. `_getIntentsMapForTesting()` exported for test cleanup.
- **Routing:** Single `decideChatMode()` in `server/lib/decideChatMode.ts` returns exactly one of the three modes plus sanitized `entityType` (pure noun, no counts/quantifiers) and optional `requestedCount` (numeric). The `sanitizeBusinessType()` function strips leading numeric tokens ("5 pubs"→"pubs", count=5) and grammatical articles ("a pub"→"pub", "an electrician"→"electrician") WITHOUT setting a count. Explicit quantifiers like "some", "several", "few" also strip without setting count. Only explicit numeric tokens set `requestedCount`. Both `routes.ts` RUN_SUPERVISOR paths and `intent-detector.ts` pass `requested_count` as a separate field in the `search_query` payload. Informational question prefixes ("who are", "what is", etc.) are checked FIRST and short-circuit to CHAT_INFO. Then entity intent is checked (before CHAT_INFO patterns) to enforce the invariant: any "find entities" input → CLARIFY_FOR_RUN or RUN_SUPERVISOR, never CHAT_INFO.
- **Trailing Location Detection:** `extractEntityAndLocation()` uses two-pass location extraction: (1) preposition-based via `LOCATION_PATTERN` (`in/near/around X`), then (2) if no location found, `extractTrailingKnownLocation()` checks if the extracted entity type ends with a known location name from `KNOWN_LOCATIONS` set. This set is built at module load from `server/data/` JSON files (uk_counties, london_boroughs, gb_devolved, ie_counties) plus ~400 extra UK/Ireland cities/regions/towns. Multi-word locations like "west sussex" are matched greedily (up to 4 words). Non-location words ("me", "here", "my area") are filtered via `NON_LOCATION_WORDS` set so "find X near me" correctly triggers clarification.
- **Location Validation (G6 Fix):** `isKnownLocation()` validates extracted locations against the `KNOWN_LOCATIONS` set before allowing RUN_SUPERVISOR. Unknown/nonsense locations (e.g. "council things") force CLARIFY_FOR_RUN with "unrecognised location" in the reason. The function uses sliding-window sub-phrase matching so "greater manchester" finds "manchester" even if the full compound isn't listed. Exported for testing.
- **Entity Type Validation (G6 Fix):** `hasConcreteEntityNoun()` checks if the entity type contains at least one concrete business noun from the `CONCRETE_ENTITY_NOUNS` regex (150+ business types). If the entity type is entirely composed of subjective/filler words ("best vibes", "amazing energy", "top feels"), `isRunnable()` returns false and routes to CLARIFY_FOR_RUN. The `SUBJECTIVE_WORDS` set includes adjectives, intensifiers, and vague nouns. Entity types with unrecognised but non-subjective words (e.g. "widget manufacturers") pass through — the check only blocks purely unmeasurable queries. **G6 clarify UX fix:** In the CLARIFY_FOR_RUN lane of `routes.ts`, vague entity types (failing `hasConcreteEntityNoun`) and invalid locations (failing `isKnownLocation`) are nulled out before creating the session, so `buildInitialQuestions` and `buildClarifyStatePayload` correctly treat them as missing. This prevents false "Ready to search" on nonsense queries and ensures the chat bubble always shows explicit questions. If all extracted values are vague/invalid (producing zero questions), a refusal message is shown instead.
- **Clarify Sessions:** `server/lib/clarifySession.ts` manages **in-memory** multi-turn clarification (no DB dependency). Sessions stored in a `Map<conversationId, ClarifySession>` with 30-min TTL and periodic GC. All session operations are synchronous. `handleClarifyResponse()` returns `ask_more | run_supervisor | cancelled` but **never closes sessions itself** — only `routes.ts` can close a session, and only after successful supervisor task creation (or explicit cancel). Semantic constraints require explicit resolution via `semantic_constraint_resolved` flag; entity_type + location alone are not sufficient if a semantic constraint exists. Active session check in `server/routes.ts` runs BEFORE the 3-way router.
- **[META_TRUST Early Interception]:** `isMetaTrustQuestion()` runs at the TOP of the chat handler, BEFORE any clarify guard or execution logic. If the message matches trust/accuracy patterns (guaranteed, accurate, reliable, how sure, where from, can I trust, source of data, what is wyshbone, can it lie, is it honest/truthful/trustworthy, how does wyshbone work, does it hallucinate/fabricate/deceive) **AND no clarify session is active**, the server immediately returns a canned honest-limitations response as a chat bubble, does NOT create a supervisor task, and completes the SSE stream. **When a clarify session IS active**, the early interception is skipped; instead the clarify guard's `classifyClarifyInput()` classifies the message as `META_TRUST`, suspends the session (not closed), re-emits `clarify_state` to keep the yellow box visible, and falls through to `decideChatMode` → CHAT_INFO for a full GPT-streamed conversational answer. This ensures meta/trust questions get real conversational responses during clarify mode instead of being swallowed by slot-filling.
- **[CLARIFY_GUARD] With Mid-Clarify Pivot Detection:** When a clarify session is active, `classifyClarifyInput()` runs to classify each message as EXECUTE, NEW_TASK, CHAT_INFO, or REFINE (META_TRUST is already handled by early interception). **EXECUTE** (run triggers, bare acknowledgements): routes to `handleClarifyResponse` as before. **REFINE** (default, including cancel phrases): routes to `handleClarifyResponse` as before. **NEW_TASK** (different entity noun than session's current entity_type, or "new question:" prefix): closes session via `closeAllClarifySessions`, emits `clarify_session_ended` SSE, then falls through to `decideChatMode` pipeline as if no session existed. **CHAT_INFO** (advice/help questions like "can you help me with sales", "how do I sell more", "any tips on marketing"): session is preserved/suspended (not closed), message falls through to `decideChatMode` which routes to CHAT_INFO for a normal chat answer. No slot mutation. CHAT_INFO is only triggered when the message has NO search verb (prevents false positives on "find me cafes"). Entity noun comparison is stem-aware (strips trailing 's'). The classifier prevents catastrophic merging where "cafes in bristol" during a pubs clarify would silently override slots.
- **Clarify Confirmation Flow:** When user provides new data that fills all required fields (e.g. providing location when entity_type was already known), the session presents a confirmation summary ("Got it — I'll search for X in Y. Click **Search now** to proceed.") instead of auto-transitioning. Transition to RUN_SUPERVISOR only happens when: (a) user sends a bare acknowledgement ("yes", "go ahead") with all fields filled, (b) user sends a run-trigger phrase ("search now", "just search", "run the search", "please search", "do the search", "launch search", etc.), or (c) user sends a message with no new data extracted and all fields are filled. Run trigger matching uses exact word boundaries (phrase + space/comma/end) to prevent false positives — "search for dentists" does NOT match the "search" trigger. **G3b fix:** All user-facing copy directs users to "click" the Search now button — no copy mentions typing "search now". The button is the sole execution affordance in the UI.
- **Error Handling:** Both RUN_SUPERVISOR paths (direct from router and from clarify→run) use try-catch around `createSupervisorTask`. If DB is down, the catch block returns a truthful "system unavailable" message. Clarify sessions are **preserved on failure** — session only closes after successful task creation. CLARIFY_FOR_RUN never touches the DB at all. No pre-flight DB health check is used (removed due to false negatives blocking valid requests).
- **Confidence Confirmation Message:** Before supervisor task creation on ALL three paths (direct RUN_SUPERVISOR, CLARIFY→RUN, FOLLOWUP_REUSE), a `type: 'confidence'` SSE event is emitted with an informational message like "Searching for 5 pubs with live music in Arundel." (title-cased location). The client renders it as a distinct blue-tinted bubble with a Search icon — no user action required. On history reload, messages matching `/^Searching for .+\.$/.test()` are auto-detected and re-styled. The old generic delegation message ("Task delegated to Supervisor...") is replaced by this confidence message.
- **Bypass Endpoints Removed:** `POST /agent/chat` and `POST /api/agent/chat` return HTTP 410 (Gone). `POST /api/tower/chat-test` is gated behind `ENABLE_DEV_CHAT_ENDPOINTS=true`.
- **Shared Infrastructure:** Both lanes use a unified execution layer and PostgreSQL for conversation history.

**UI/UX Decisions:**
The user interface adheres to Material Design principles, featuring a dark mode, Inter font, consistent spacing, real-time chat, auto-expanding input, theme toggle, and a collapsible sidebar with a default UK country selector. Accessibility (WCAG AA) is a key consideration.
- **New Chat / Load Conversation State Reset:** Both `handleNewChat` and `handleLoadConversation` in `chat.tsx` perform a full state teardown: abort active streams, clear supervisor timers (timeout + poll interval), reset `isClarifyingForRun`, `activeClientRequestId`, `supervisorTaskId`, `isWaitingForSupervisor`, and all context request IDs (`current/pinned/lastCompleted`). This prevents the "Clarifying before run" badge, Live Activity panel data, and supervisor state from leaking across conversations.
- **Google Query Mode Toggle:** Header bar pill toggle (Fast/Stable) controls `google_query_mode` sent to Supervisor. Persisted in `localStorage` key `wyshbone.google_query_mode`. Values: `TEXT_ONLY` (Fast) or `BIASED_STABLE` (Stable, default). Component: `client/src/components/GoogleQueryModeToggle.tsx`. Wired into all three supervisor request paths (direct, clarify, followup-reuse) via `requestData.google_query_mode`. Validated in `chatRequestSchema` (shared/schema.ts). Shown in diagnostic panel.

**Technical Implementations & Features:**
- **AI Chat Interface:** Real-time conversations with an AI assistant, enforcing a concise, practical, and UK-focused personality.
- **Agentic V1 Workflow:** Goal-driven initialization for sales/lead goals, plan generation, approval, and execution monitoring.
- **Unified Action Layer:** Four core functions (DEEP_RESEARCH, SEARCH_PLACES, CREATE_SCHEDULED_MONITOR, BATCH_CONTACT_FINDER) execute identically across AI modes.
- **Job Management & Worldwide Location Coverage:** Background job system for global searches, intelligent location resolution, and natural language job creation.
- **Streaming Responses:** Uses Server-Sent Events (SSE) for real-time AI responses.
- **Conversational Planning:** GPT-based planner decides whether to search, use cache, or respond.
- **Deep Research Context Extraction & Auto-Summarize:** Server-side prompt enhancement and GPT-4o summarization for research reports.
- **Persistent Memory System:** Database-backed conversation history and knowledge accumulation.
- **Scheduled Monitors:** Agentic monitoring system for recurring tasks with flexible scheduling, email notifications, and "Smart Summary Mode."
- **Xero OAuth Integration:** Direct OAuth 2.0 integration for secure Xero accounting connections.
- **Batch Contact Discovery Pipeline:** Cost-optimized contact finding using multiple external services.
- **Stripe Subscription System:** Freemium model with paid tiers, managing user subscriptions and usage limits.
- **Agent Flight Recorder (AFR) & Live Activity Panel:** User-facing system showing real-time decision paths and execution progress via correlation IDs, idempotent run creation, transparent router decision logging, and a unified timeline of events. The run lifecycle includes a `clarifying` status: when CLARIFY_FOR_RUN is chosen, the run transitions from `starting → clarifying` and persists a `clarify_gate` artefact. When a constraint is resolved (subjective/numeric/relationship), a `clarify_resolution` artefact is persisted. When proceeding to supervisor, the run transitions `clarifying → executing`. Clarifying runs are exempt from the 5-minute stale-run auto-fail. The StatusBadge shows an amber "Clarifying" badge with MessageSquare icon. The TruthStrip recognizes `clarify_gate` artefacts and shows Tower verdict as "N/A (clarifying)" with a neutral amber Clock icon instead of red X. **AFR-Poll Clarify Gate Detection:** The AFR polling loop (1.5s interval) now detects `status === 'clarifying'` from `/api/afr/stream`. When detected, it fetches `/api/afr/artefacts` for the run, finds the `clarify_gate` artefact, extracts `reason`, `options`, `constraint_type`, and `constraint_label` from the payload, then: (1) sets `isClarifyingForRun=true` with a populated `clarifyContext` including the mapped `ConstraintContract`, (2) inserts an assistant chat message with the clarification question (id=`clarify-gate-{key}`), (3) removes any provisional "Checking requirements" spinner bubble. This prevents the UI from hanging indefinitely when the Supervisor gates a run for clarification. Uses existing artefact API — no new endpoints. Constraint types are mapped: `subjective_term→subjective`, `numeric_predicate→numeric_ambiguity`, `relationship_predicate→relationship_predicate`, `time_predicate→time_predicate`. The `clarifyDetected` set prevents re-surfacing on subsequent polls. **AFR-Poll Failure Bail-Out:** Safeguards handle long-running or failing polls: (1) **Soft timeout** (30s + 5 consecutive failures) — shows non-terminal `still_working` blue banner; polling continues. (2) **Hard timeout** (90s) — shows non-terminal `still_working` blue banner; polling continues. Both timeouts are NON-TERMINAL: they do NOT stop polling and do NOT mark the run as failed. (3) **Consecutive API failures** (20) — if `/api/afr/stream` is unreachable 20 times in a row, emits terminal `run_not_persisted` error bubble, stops polling. (4) **Terminal status + no artefacts** — if backend says terminal but delivery_summary not found after 10 retries, emits terminal `artefacts_unavailable` error. (5) **activeClientRequestId clear** — only truly terminal failure paths clear `activeClientRequestId` and `isStreaming`. `still_working` banners do NOT clear these.
- **Canonical Tower Verdict Resolver (`client/src/utils/towerVerdictResolver.ts`):** Single source of truth for resolving Tower verdicts from `tower_judgement` artefacts. Used by chat bubble (`chat.tsx`), Live Activity Panel (`live-activity-panel.tsx`), and artefact dialog. Resolution rules: (1) If a `final_delivery` phase judgement exists with pass verdict, use it — the run-level pass overrides step-level failures. (2) If only one judgement exists, use it as-is. (3) If multiple judgements with mixed pass/fail and NO `final_delivery` phase, display "MIXED (N steps failed)" instead of silently overriding. The `isTowerTrustFailure()` helper returns true only for unambiguous fail/error/stop verdicts. Mixed verdicts are NOT treated as trust failures. All three display locations (chat banner `TrustErrorBlock`, Live Activity `SequenceStatusRow`, artefact dialog `TowerVerdictBadge`) use this shared resolver to guarantee consistency.
- **Human Summary + Technical Details (`HumanSummary` / `TechnicalDetails` / `buildRunNarrative`):** Two-tier chat bubble output for lead-finder runs. Default view is a plain-English `HumanSummary` (3–6 prose lines: "I searched Google Places for pubs in Arundel and found 30 possible matches…"). No jargon words (Tower, artefact, constraint, CVL, etc.) appear in the default view. All technical content — Tower verdict, SearchSummaryBlock, constraints, verification counts, stop reason, plan timeline, policy/learning sections, and diagnostics — is hidden behind a `TechnicalDetails` component with a "Show details" / "Hide details" toggle (collapsed by default). The trust-failure red banner (`TrustErrorBlock`) still appears only for FAIL/STOP, but lead listings now show even on FAIL runs. `buildRunNarrative()` is a pure exported function producing `RunNarrative { lines[], isTrustFailure, counts, contactCountsSource }` for testability. **Contact counts** are derived from real artefacts via `ContactCounts` interface, **scoped to delivered leads only**. `extractContactCounts(rows, deliverySummary)` builds a set of delivered entity_ids (from `delivered_exact[*].entity_id`) and names, then filters `lead_pack` artefacts by matching `identity.place_id` or `identity.name`/`inputs.entity_name` against delivered leads. Only matched artefacts contribute to counts. Proven payload paths: `lead_pack` at `payload_json.outputs.lead_pack.contacts.emails[*].value` (objects) / `.phones[*].value`; `contact_extract` at `payload_json.outputs.contacts.emails[*]` (plain strings) / `.phones[*]`. `delivery_summary.delivered_exact` has NO contact fields. Email validation uses regex extraction + dedup; phone validation requires 8+ digits. `ContactCounts` includes `emails[]` and `phones[]` arrays plus rich `debugInfo` (total/matched artefact counts, delivered lead count, used artefact IDs, mapping notes). "Show details" shows matched/total ratios, redacted emails (***@domain), and masked phones.
- **Receipt Comparison (debug feature):** Two display modes, both DEV-only. (1) **Inline bubble line** (`ReceiptInlineLine`): gated by `VITE_SHOW_RECEIPT_IN_BUBBLE=true` AND `MODE !== 'production'`. Shows "Receipt (backend): emails=N phones=N proven=true" directly under the HumanSummary prose. If counts differ from UI-derived, shows amber "WARNING: mismatch vs receipt" with both pairs. If no receipt artefact, shows "Receipt (backend): not available". If `contacts_proven=false`, shows "contacts unknown (proven=false)". (2) **TechnicalDetails panel** (`ReceiptComparison`): gated by `VITE_SHOW_RECEIPT_COMPARISON=true`. Shows derived vs receipt counts side-by-side in the expandable details section. `RunReceipt` interface: `{ unique_email_count, unique_phone_count, contacts_proven, requested_count, delivered_count, narrative_lines, tower_verdict }`. Extracted from `type=run_receipt` artefact in `parseSiblingArtefacts()`. Tower verdict sourced from `tower_judgement` artefacts where `artefact_type=run_receipt`.
- **Non-Terminal Timeout (still_working):** Timeouts are NON-TERMINAL. When >30s elapses with 5+ consecutive API failures (soft timeout) or >90s elapses (hard timeout), the UI emits a `still_working` banner (blue, with spinner: "Still working — this search is taking longer than usual. Results will appear when ready.") but does NOT stop polling, does NOT add the key to `deliverySummaryRunIdsRef`, and does NOT call `cleanupRunState`. Polling continues until the backend sends an explicit terminal status (`is_terminal`, `completed`, `failed`, `PASS`/`FAIL`/`STOP`). When real results arrive via `finalizeRunUI`, the `upsertResultMessage` replaces the `still_working` banner (same `ds-${key}` id) with the actual result bubble. The only truly terminal client-side stop reasons are: `run_not_persisted` (20 consecutive API failures = backend unreachable) and `artefacts_unavailable` (terminal status received but no delivery_summary after 10 retries). The `still_working` stop_reason uses `status: 'PENDING'` (not `'FAIL'`), so `buildRunNarrative` won't generate "No results could be delivered" text.
- **Constraint Hardness Display:** `HardnessBadge` in `CvlArtefactViews.tsx` reads `c.hardness` directly from backend artefacts without defaulting. If `hardness` is "hard" → red Hard badge. If "soft" → amber Soft badge. If missing/unknown → gray Unknown badge. The `CvlConstraint.hardness` field is optional to avoid silent defaults.
- **UNKNOWN Attribute Verification Banner:** When Tower verdict is PASS but attribute constraints have UNKNOWN verification status, a blue info banner is shown: "I found results, but I couldn't verify which ones mention [attribute] on their website." This is distinct from the red trust failure banner (Tower FAIL) and the amber mixed-verdict banner.
- **Attribute-Aware Narrative (`buildAttributeOutcomes` + `buildRunNarrative`):** The chat bubble narrative now uses the same verification data as the Results modal. `buildAttributeOutcomes()` in `RunResultBubble.tsx` takes `constraintsExtracted` (for HAS_ATTRIBUTE constraints) and `leadVerifications` (per-lead constraint checks) — the exact same data the modal uses — and produces `AttributeOutcome[]` with `{ attribute_raw, matched_count, unknown_count, total_checked, matched_lead_ids }`. `buildRunNarrative()` uses these outcomes to add attribute-specific lines: e.g., '3 of their websites mention "live music".' or 'I wasn't able to verify which ones mention "live music" on their website.' This data does NOT come from `run_receipt`. Existing derived email/phone counts are unchanged.
- **User Results View (`UserResultsView`):** Standalone component rendering run outcomes from `delivery_summary` objects.
- **What Was Learned Panel (`WhatWasLearnedPanel`):** Component displaying insights from `RuleUpdate` data.
- **CVL V1 Artefact Renderers:** Five new artefact types rendered in LiveActivityPanel: `constraints_extracted`, `constraint_capability_check`, `verification_summary`, `verification_evidence`, `lead_verification`.
- **Learning Layer v1 (Telemetry + Policies UI):** Fire-and-forget telemetry system for user interaction events linked to `run_id`.
- **RunResultBubble Durability:** Structured results are persisted to the messages table via a JSONB `metadata` column for hydration on page reload.
- **Tower Judgement Resolution (Final Delivery Authoritative):** When multiple `tower_judgement` artefacts exist for a run, the system prioritises `phase=final_delivery` judgements as the single source of truth. If a `final_delivery` judgement exists with verdict PASS/ACCEPT/ACCEPT_WITH_UNVERIFIED, it overrides any earlier intermediate FAIL verdicts (e.g. from WEB_VISIT captcha failures) — fail stickiness does NOT apply when final_delivery is passing. If no `final_delivery` judgement exists, the system falls back to the last judgement with original fail stickiness. If `final_delivery` is FAIL/STOP, the red trust-error banner is shown. Implemented via `resolveAuthoritativeTower()` helper in `chat.tsx`, `deriveTowerAwareStatus` in `live-activity-panel.tsx`, EvidenceSection (preloaded/fetch paths), and ResultsModal towerPayload resolution. `isTrustFailure` in both `chat.tsx` and `RunResultBubble.tsx` checks `isFinalDeliveryPass` before triggering failure state. Artefact ordering is guaranteed by API (`ORDER BY created_at ASC`).
- **Location Verification Buckets:** When Supervisor delivers leads with `location_status` field (on every lead), RunResultBubble switches from the binary matches/candidates split to four location-based buckets: Verified (geo) = `VERIFIED_GEO`, Search-bounded = `SEARCH_BOUNDED` (blue, with Radar icon, "likely in area"), Out of area = `OUT_OF_AREA` (red), Unknown = `UNKNOWN` (gray). Constraint-verification status (`isVerified`) is preserved per-lead within each bucket via the existing `matchSet` logic. Falls back to the original matches/candidates display when leads lack `location_status`. Requires ALL leads to have the field for bucket mode to activate (partial data uses legacy display).
- **Trust-Neutral Result Language:** RunResultBubble uses neutral, factual language that avoids implying system failure. Key rules: (1) No arbitrary lead splitting — `splitLeadsByVerification` only uses actual `lead_verification` data or `verifiedExact >= allLeads` count; never slices by position. (2) Section headers say "Results (N)" or "Other results (N)" instead of "Candidates (not fully verified)"; for STOP/PARTIAL statuses, headers always say "Results" not "Matches". (3) Summary text for PARTIAL/STOP with tower verdict PARTIAL/STOP uses honest "candidates, but could not verify the requirement" framing; without tower verdict uses neutral framing ("I found N results for your search"). (4) Unverified leads show no badge (null) instead of "Candidate". (5) Unverifiable attribute badge says "X not checked" instead of "Not confirmed: X". (6) Next action buttons use exploratory framing ("Search more results", "Try a different search") with context-aware heading ("Next steps" when target count specified, "Explore further" otherwise). (7) STATUS_CONFIG labels: PARTIAL="Results returned", STOP="Search stopped". (8) LeadBadge shows "Verified" instead of "Verified match". (9) FAIL status explicitly says "Search could not be completed. Results were not verified." (10) **ACCEPT_WITH_UNVERIFIED contradiction fix:** When Tower sends PASS but leads are unverified (no `lead_verification` data, or `matches < allLeads`), the effective status is downgraded from PASS (green) to ACCEPT_WITH_UNVERIFIED (amber warning). Tower can also send `ACCEPT_WITH_UNVERIFIED` verdict explicitly. Status uses AlertTriangle icon, amber color, "Results (N)" header (not "Matches"), and summary text mentioning "not all could be verified." STATUS_CONFIG, STATUS_ICONS, STATUS_COLORS updated in RunResultBubble.tsx and UserResultsView.tsx. Tests T21–T25 cover all paths.
- **Learning Plan UI (Part 3 MVP):**
  - **Pre-Run Banner:** "Why this approach" banner appears in chat before results arrive for any RUN_SUPERVISOR run. Shows applied policy knobs (result_count, verification_level, search_budget_pages, radius_escalation) with source badges (default/user override/learned). Only claims "learned" when `policy_applied.learned_used=true`. Shows "Using default settings" when `learned_used=false`. Placeholder text is neutral "Loading run settings…" (no learning claims). Implemented in `client/src/components/results/PreRunBanner.tsx`.
  - **Applied Policy Section (RunResultBubble):** Replaced generic "Learning" section header with "Applied policy". Shows policy knobs with source badges. Only shows "Learned settings applied" when `policy_applied.learned_used=true` with source run IDs. Shows "Using default settings" when `learned_used=false`. No learning claims without `policy_applied` artefact.
  - **Learning Delta Section (RunResultBubble):** "What changed since last time" section only appears when `learning_update` artefact present. Shows `changed_fields[]` with before→after values, `tower_reason`, and `metrics_trigger`. Implemented inline in `RunResultBubble.tsx`.
  - **Verification Step Labeling:** Multiple "Searching for…" confidence bubbles now show "Verification step 1/N" labels instead of implying learning. Single searches show no step label.
  - **Policy Formatters:** Helper utilities in `client/src/utils/policyFormatters.ts` — interfaces for `PolicyApplied`, `LearningUpdate`, and `SearchQueryCompiled`, source badge styling, knob label mapping, value formatting, metrics trigger formatting, confidence text builder (`buildCleanConfidenceText`), and safe parsers (`parsePolicyApplied`, `parseLearningUpdate`, `parseSearchQueryCompiled`).
  - **Artefact Parsing:** `parseSiblingArtefacts()` in `chat.tsx` now extracts `policy_applied`, `learning_update`, and `search_query_compiled` artefact types. All flow through to `RunResultBubble` props and are persisted in message metadata for history hydration.
  - **Search Summary Block (RunResultBubble):** Compact `SearchSummaryBlock` rendered in `RunResultBubble` when `search_query_compiled` artefact is present. Shows Location (interpreted_location), Query (interpreted_query), Target (requested_count), Got (final_returned_count), What was tried (radius_escalated, pages_budget_used, query_broadened), and Stop reason. Underfilled runs highlight the Got count in amber. Only shown when not provisional.
  - **Confidence Text Sanitization:** Incoming SSE `confidence` messages and historical "Searching for…" messages are sanitized to strip "and return exactly" contamination from raw user text. When `search_query_compiled` artefact arrives at finalization, all existing confidence messages are rewritten using `buildCleanConfidenceText()` to use interpreted_location + interpreted_query only.
  - **Stop Reason Mapping:** Raw stop reason codes mapped to user-friendly text with expandable raw code disclosure. Implemented in `client/src/utils/stopReasonMap.ts` and `client/src/components/results/StopReasonBadge.tsx`.
  - **Plan Version Timeline:** Minimal v1/v2/etc timeline showing "what changed" summaries for multi-replan runs. Implemented in `client/src/components/results/PlanVersionTimeline.tsx`.
  - **Run Config Overrides:** Per-run controls (Faster/Balanced/Stricter speed mode, replan ceiling 0-3, "Ignore learned policy for this run") near chat input. Overrides sent as `run_config_overrides` in the `/api/chat` request body. Implemented in `client/src/components/results/RunConfigOverridesPanel.tsx`.
  - **Learning Dashboard:** Full page at `/learning` accessible from main sidebar navigation. Shows scope key selector, effective policy bundle, policy history list, active rule updates with accept/reject actions (UI-only with TODO for backend endpoint), and recent outcome summary. Uses existing AFR data functions. Implemented in `client/src/pages/learning.tsx`.
  - **Feedback Buttons:** Helpful/Not helpful buttons on RunResultBubble. "Not helpful" shows reason picker (wrong businesses, constraint misunderstood, missing results, evidence wrong). POSTs to `/api/feedback` (fire-and-forget, endpoint may not exist yet). Implemented in `client/src/components/results/FeedbackButtons.tsx`.
  - **Tower Not Available Badge:** Shows "Tower not available" badge when tower unavailable signal exists in artefacts.
- **Tower Analytics Integration:** Comprehensive logging for all chat interactions using a unified `runId` system.
- **Hacker News Discovery:** Feature for finding relevant Hacker News discussions, offering AI-powered draft reply generation and relevance scoring.
- **Delivery Management System:** Mobile-first Driver UI with role-based access control.
- **Multi-Tenant Organisation System:** Production-grade organisation-based roles system with invite flow and server-side org isolation.
- **User Role System:** Formal role-based access control (admin, sales, driver) based on org membership.

**Database Configuration:**
- Uses Supabase PostgreSQL exclusively via `SUPABASE_DATABASE_URL`.
- The server crashes if `SUPABASE_DATABASE_URL` is missing.
- `runStartupMigrations()` handles schema drift.

**Thin Client Architecture:**
- The UI server must NEVER execute leadgen tools. The ONLY execution path is creating a `supervisor_tasks` row in Supabase.
- All legacy execution endpoints return 501.
- CHAT lane is pure GPT-5 streaming with no tools.
- RUN lane inserts `supervisor_tasks` row and returns immediately; Supervisor backend handles the full agent loop.

**Supervisor Completion & Delivery Pipeline:**
- Supervisor writes artefacts directly; the UI server does not run a completion poller.
- Client-side polls `/api/afr/artefacts` for `delivery_summary` when `isWaitingForSupervisor=true`.
- Rich result bubble (`RunResultBubble`) rendered inline in chat for RUN lane `delivery_summary` artefacts.
- Two-State Results Bubble: `RunResultBubble` has a `provisional` prop for provisional rendering before final `delivery_summary`.
- Terminal Run Fallback: When polling finds leads but no delivery_summary, mission-level artefacts (`run_summary`, `outcome_log`, `policy_application_snapshot`, `verification_summary`, `run_halted`, `tower_judgement`) count as terminal. No time-based finalisation — provisional bubbles wait for delivery_summary or mission-terminal artefact.
- Realtime Subscription Robustness: `subscribeSupervisorMessages` listens on both INSERT and UPDATE events. The realtime callback only cleans up run state and stops polling when `delivery_summary` is actually found; on failure it leaves the polling loop active. Processed message IDs are deduped via `processedRealtimeMsgIdsRef`. A `finalizeResultsUI()` safety net force-fetches messages from the DB 1s after terminal detection to catch any messages missed by realtime.
- Orphaned Run Recovery: On page load (after history loads), a recovery effect fetches recent `/api/afr/runs?conversation_id=<current>`, finds completed runs for the current conversation that don't have result bubbles rendered, fetches their artefacts, and creates final result messages. Recovery is conversation-scoped to prevent cross-conversation contamination. The `/api/afr/runs` endpoint supports `conversation_id` and `user_id` query parameters for filtering.
- Activity Panel Completion Lifecycle: `wyshbone:results_final` CustomEvent sets `userVisibleComplete=true` for immediate "Completed" status.
- Late Event Suppression: Freeze is a **rendering-only** concern. All events are always ingested into `allEvents`/`rawEvents` for state derivation (terminal detection, tower-aware status, mapped status). Only the `frozenDisplayEvents` array (fed to `usePacedPlaybackQueue` for rendering) is truncated after Phase 1. Phase 2 ("artefacts saved" tick) requires `effectiveTerminal` — no timeout fallback.
- Polling Fetch Pattern: `fetchStream` uses a "skip if in-flight" semaphore (`fetchInFlightRef`) instead of aborting previous requests. This prevents a race condition where the ~1.2s server response time overlaps with the 1.5s polling interval, causing abort-before-parse failures that leave the stream stuck on stale data. Cache-busting (`_t` timestamp param, `cache: 'no-store'`) and server-side `Cache-Control: no-store` headers on `/api/afr/stream` prevent HTTP caching of terminal state transitions.
- Supervisor Bubble Suppression Guard: Prevents contradictory free-text chat bubbles from appearing with `delivery_summary` results.

**AFR Artefact Ingestion & Retrieval Contract:**
- **POST `/api/afr/artefacts`**: Persists an artefact.
- **GET `/api/afr/artefacts`**: Retrieves artefacts.
- **POST `/api/afr/run-bridge`**: Links a Supervisor run to the canonical UI `runId`.

**Data Ownership & Persistence Guardrails:**
The UI never owns persistence. All artefacts, runs, judgements, and business data come from Supabase via the backend. The frontend is a read/display layer only; it does not write directly to any database. All data mutations flow through backend API endpoints to Supabase PostgreSQL. `SUPABASE_DATABASE_URL` is the single source of truth for database connectivity.

## Subjective Constraint Gate
- **Detection:** `extractSubjectiveModifiers()` and `hasSubjectiveModifiers()` in `decideChatMode.ts` detect subjective adjective modifiers (e.g., "nice", "good", "cool", "best") in entity types. When a concrete entity noun has subjective modifiers (e.g., "nice bars"), `isRunnable()` returns false → CLARIFY_FOR_RUN.
- **Constraint Contract:** Creates a `type: 'subjective'` constraint contract with `can_execute: false`, `why_blocked` quoting the subjective term(s), `subjective_terms` array, and `subjective_options` array of measurable alternatives (Lively, Quiet, Cosy, Late-night, Live music, Good for food, Beer garden, Dog friendly).
- **Clarification Question:** Generates "What do you mean by 'nice'?" (or "'nice' and 'good'" for multiple terms) in the chat thread.
- **Option Buttons:** UI renders subjective options as rounded pill buttons in the constraint clarification panel. Clicking one sends the option as a user message through the clarify pipeline.
- **Resolution:** `handleClarifyResponse()` in `clarifySession.ts` matches subjective option values with fuzzy matching (strips trailing punctuation, allows "option + noun" and "noun + option" variants). Imports shared `SUBJECTIVE_WORDS` set from `decideChatMode.ts` for entity type cleanup (single source of truth). Strips subjective words from the entity type, resolves the semantic constraint, and flips `can_execute` to `true`. After resolution, calls `advanceToNextConstraint()` to pop the next pending constraint from the queue.
- **Gate Enforcement:** While `can_execute === false`: "Search now" button hidden, Enter-to-run allowed only for clarification responses (not execution), no auto-trigger of Supervisor, no "Searching…" UI.
- **Append-only:** The clarification question and user's choice remain visible in the chat thread.
- **UI Tests:** `tests/subjective-gate.spec.ts` covers: T1 (renders why_blocked + options), T2 (Search now hidden), T3 (option resolves gate), T4 (time-predicate regression), T5 (multiple terms), T6 (non-subjective passthrough), T7 (compound subjective+time sequences correctly), T8 (pure time-predicate immediate), T9 (subjective-only resolves to ready). All 9 pass.

## Constraint Sequencing (Single-Blocker State Machine)
- **Architecture:** The CLARIFY_FOR_RUN path implements a true single-blocker state machine. Only one constraint is active at a time; others are queued in `pending_constraints[]` on `ClarifySession`.
- **Priority order:** `subjective` (0) > `numeric_ambiguity` (1) > `relationship_predicate` (2) > `time_predicate` (3) > `unverifiable_constraint` (4). Implemented via priority sort in `routes.ts` CLARIFY_FOR_RUN lane.
- **Session field:** `ClarifySession.pending_constraints: PendingConstraintDescriptor[]` stores deferred constraint descriptors (type, contract, questions, constraintLabel).
- **Question isolation:** Only the active constraint's question(s) appear in `pendingQuestions[]`. Deferred constraints' questions are NOT shown until their turn.
- **Advancement:** `advanceToNextConstraint()` in `clarifySession.ts` pops the next descriptor, sets it as the active `constraint_contract`, resets `semantic_constraint_resolved` to false, and replaces `pending_questions` with the next constraint's questions. Called after every constraint resolution (subjective, proxy, attribute, generic).
- **Ready gate:** `buildClarifyStatePayload` and `allRequiredFieldsPresent` both check `pending_constraints.length > 0` — status cannot be `'ready'` while queued constraints remain.

## Numeric Ambiguity Gate
- **Detection:** `hasNumericAmbiguity()` and `extractNumericAmbiguityTerms()` in `decideChatMode.ts` detect vague quantity words: "few", "some", "several", "many", "couple", "most", "top", "numerous". When detected, `isRunnable()` returns false → CLARIFY_FOR_RUN.
- **Explicit numbers bypass:** "Find 5 pubs in Brighton" — the explicit `5` is parsed by `sanitizeBusinessType` as `requestedCount`, and `hasNumericAmbiguity` does NOT trigger for bare digits → RUN_SUPERVISOR immediately.
- **Constraint Contract:** Creates a `type: 'numeric_ambiguity'` constraint contract with `can_execute: false`, `why_blocked` quoting the vague term, and `numeric_options: ['3', '5', '10', 'All']`.
- **UI rendering:** Numeric options render as rounded pill buttons (like subjective options). No proxy options, no "Relax certainty" button, no subjective options — only numeric buttons and "Change my query".
- **Resolution:** `handleClarifyResponse()` matches user input against `NUMERIC_OPTION_VALUES` ('3', '5', '10', 'all'). Sets the chosen count in `answers.count` and flips `can_execute: true`. Then calls `advanceToNextConstraint()`.
- **Priority:** numeric_ambiguity has priority 1 (after subjective=0, before relationship_predicate=2, time_predicate=3, unverifiable_constraint=4).
- **Overlap with subjective:** Words like "top" and "some" appear in both `SUBJECTIVE_WORDS` and `NUMERIC_AMBIGUITY_WORDS`. "top" triggers subjective gate (priority 0) first; "some" is excluded from `extractSubjectiveModifiers()` (articles filter), so only triggers numeric gate.
- **"cheap" is subjective:** Added "cheap", "cheapest", "affordable", "budget", "inexpensive" to `SUBJECTIVE_WORDS` — these are quality judgements, not quantities.
- **UI Tests:** `tests/numeric-ambiguity-gate.spec.ts` covers: T10 ("few pubs" triggers numeric_ambiguity), T11 ("several bars" triggers), T12 (explicit "5 pubs" no gate), T13 (answering "5" resolves gate), T14 (answering "all" resolves), T15 (no proxy/subjective options in contract), T16 (nice+few = subjective first), T17 (cheap = subjective). All 8 pass.

## Relationship Predicate Gate
- **Detection:** `hasRelationshipPredicate()` in `decideChatMode.ts` detects two categories of relationship patterns. When detected, `isRunnable()` returns false → CLARIFY_FOR_RUN.
  - **Verb patterns:** "that work with", "that partner with", "who collaborate with", "that deal with", "that specialise in", "that specialize in", "who supply", "that serve".
  - **Role/ownership patterns (Batch 4):** owner, owned by, landlord, manager, operator, run by, managed by, operated by, part of, group, chain, decision maker, practice manager, head brewer, contact name. Detected via `RELATIONSHIP_ROLE_TERMS` and `RELATIONSHIP_ROLE_PATTERN` regex, with optional "and the" / "with the" prefix and optional trailing "name".
- **Constraint Contract:** Creates a `type: 'relationship_predicate'` constraint contract with `can_execute: false`, `why_blocked` quoting the relationship phrase, and `relationship_options: ['Official sources only', 'Best-effort public web', 'Require 2+ sources', 'Skip if uncertain']`.
- **UI rendering:** Relationship options render as list buttons under "Verification approach" header. No proxy options, no "Relax certainty" button, no subjective/numeric options — only relationship buttons and "Change my query".
- **Resolution:** `handleClarifyResponse()` in `clarifySession.ts` matches user input against relationship option values (case-insensitive). Sets the chosen verification approach in the semantic constraint resolution and flips `can_execute: true`. Then calls `advanceToNextConstraint()`.
- **Priority:** relationship_predicate has priority 2 (after subjective=0, numeric_ambiguity=1; before time_predicate=3, unverifiable_constraint=4).
- **UI Tests:** `tests/relationship-predicate-gate.spec.ts` covers: T19 (triggers with relationship_options), T20 (can_execute false), T21 ("official sources only" resolves), T22 ("best-effort public web" resolves), T23 (no proxy/subjective/numeric leak), T24 ("that specialise in" triggers), T25 ("and the landlord name" → CLARIFY not RUN), T26 ("and the practice manager" triggers), T27 ("owned by" triggers), T28 ("skip if uncertain" resolves). All 10 pass.

## Opened-Time Predicate Gate (Honesty Gate)
- **Detection:** `hasOpenedTimePredicate()` in `decideChatMode.ts` detects phrases like "opened in the last 12 months", "opened recently", "opened this year", "just opened", "newly opened". When detected without an explicit proxy, `isRunnable()` returns false → CLARIFY_FOR_RUN.
- **Explicit Proxy Bypass:** `hasExplicitProxy()` detects "using X as a proxy" phrases. When both time predicate and explicit proxy are present, execution is allowed (RUN_SUPERVISOR) with `opened_proxy` and `proxy_disclaimer` fields in `search_query`. The confidence message includes "Using proxy: X (not guaranteed)."
- **No-Proxy Refusal:** `hasNoProxyRefusal()` detects "no proxies", "must be certain", "no guessing". When detected with a time predicate, the system returns an immediate refusal explaining that opening dates cannot be guaranteed, and suggests alternative proxy approaches.
- **Proxy Clarification Questions:** When CLARIFY_FOR_RUN is triggered by a time predicate, the clarify session includes a question listing proxy options (first Google review date, news mentions, Google Maps listing freshness, Companies House incorporation date). The time predicate is stored as a semantic constraint on the session.
- **Compound Attribute Constraints:** `extractAttributeConstraints()` in `decideChatMode.ts` detects compound attribute clauses ("and have live music", "and serve vegan food", "with beer garden"). When both a time predicate AND attribute constraints are present, the CLARIFY_FOR_RUN handler generates separate questions for each: one for the opening-date proxy choice and one for each attribute verification standard (best-effort / strict filter / skip). `SEMANTIC_CONSTRAINT_PATTERNS` also includes `and have/are/do/...` compound patterns so `hasSemanticConstraint()` catches them in `isRunnable()`. Both constraints are stored in a combined `semantic_constraint` string (e.g., "opened-time predicate + attribute: live music") which blocks execution until resolved.
- **Constraint Contract (`can_execute` / `why_blocked`):** `ClarifySession` now carries a `constraint_contract: ConstraintContractData | null` field. When a session has unresolvable constraints (time predicate, unverifiable attributes), the contract is created with `can_execute: false`. If user says "must be certain", `handleClarifyResponse` sets `why_blocked` explaining the block and offering safe next actions. `buildClarifyStatePayload` includes the contract in the SSE payload. When user picks a proxy or relaxes certainty, the contract flips to `can_execute: true` and `status` can transition to `ready`.
- **UI `why_blocked` gate:** When `clarifyContext.constraintContract.can_execute === false`: (1) "Search now" button is never shown (both inline and panel); (2) `why_blocked` message renders prominently with red/warning styling and AlertTriangle icon; (3) `suggested_rephrase` renders below `why_blocked` as italic hint text ("Try: …"); (4) Safe next action buttons appear: "Choose a proxy" (if proxy_options exist), "Relax certainty (best-effort)", "Change my query (start over)"; (5) Pending questions always render when blocked (even if `missingFields` is empty). All gating is type-agnostic — works identically for `time_predicate`, `subjective_predicate`, `unverifiable_constraint`, and any future type.
- **Append-only chat transcript:** `isClarifySuperseded` no longer hides previous clarify messages. All assistant bubbles remain visible regardless of router path changes. Deep research confirmation messages are the only exceptions (ephemeral action prompts).

## Slot-Fill Rules (Clarification Sessions)
- **Bare numbers** (e.g. "5") are always treated as count slot-fills, never rejected as "not meaningful"
- **Location override**: Explicit location patterns ("in York", "York") are parsed even when session already has a location set, allowing overrides
- **Fuzzy location fallback**: Short unrecognized text (≤3 words) is only auto-interpreted as location when session has NO location yet
- **No field erasure**: Previously collected fields are never discarded unless the user explicitly contradicts them

## Recent Changes (Feb 2026)
- **Recovery Pipeline Fix:** Rewrote orphaned run recovery to use `/api/afr/runs` endpoint (instead of `/api/afr/activities`) — fixes status field mismatch (`completed` vs `success`) and enables proper `client_request_id` correlation.
- **AFR Terminal Inference:** Server-side `/api/afr/stream` now infers `is_terminal: true` when `status === 'completed'/'failed'` even if `terminalState` is null, fixing Activity Panel terminal detection.
- **Unified `finalizeRunUI()`:** Consolidated three delivery detection paths (polling, realtime, recovery) into a single idempotent function for creating/updating result bubbles.
- **Activity Panel Bridge:** Added `wyshbone:activity_terminal` CustomEvent dispatch for coordinating Activity Panel completion state.
- **Duplicate Key Handling:** Fixed DrizzleQueryError handling in result-message endpoint — now correctly detects `error.cause.code === '23505'` for Postgres duplicate key errors wrapped by Drizzle ORM.
- **Removed Stale Timeout:** Removed destructive 2-minute stale timeout that was killing supervisor tracking state before runs could complete.
- **Conversation-Scoped Recovery:** `/api/afr/runs` endpoint now supports `conversation_id` and `user_id` query parameters. Client recovery passes current `conversationId` to prevent cross-conversation contamination. User scoping is enforced unless `all=true` is specified.
- **Reduced Dev Polling:** Plan polling reduced from 500ms to 2s, execution polling from 300ms to 1.5s in development mode to reduce log noise.
- **Post-Terminal Catch-Up Polling:** Fixed Activity Panel stalling by adding an 8-second catch-up polling window after terminal detection. When the Supervisor marks a run as completed before all activities are persisted, the panel now continues polling at 2s intervals to catch late-arriving events. Frozen display events use ID-based comparison and bypass freezing during catch-up to ensure all events render.
- **Artefact Retry Button:** When artefact fetch exhausts retries and shows a FAIL bubble with `stop_reason='artefacts_unavailable'`, a user-visible retry button dispatches `wyshbone:retry_artefacts` CustomEvent. Chat handler clears idempotency guards and re-runs `finalizeRunUI` up to 10 times with 500ms delays.
- **SSE Line Buffer Fix:** SSE parser in `chat.tsx` now uses a `sseLineBuffer` accumulator so `data:` lines split across TCP chunks are correctly reassembled before parsing. Remaining buffer is also drained after the read loop exits, preventing dropped `run_id` or `supervisorTaskId` events.
- **Error Bubble runId Fix:** Error bubbles created when artefact retries exhaust now include `runId`, enabling the retry button in `RunResultBubble` to pass the correct run identifier back to the retry handler.
- **Retry Handler Ref Fix:** The `wyshbone:retry_artefacts` handler now reads `detail.clientRequestId` from the event (alongside `detail.runId`) instead of relying on `supervisorClientRequestIdRef.current`, which was already nulled by `cleanupRunState` before the retry fires.
- **Parallel Send Block (Part A):** `handleSend` now blocks new sends while `isStreaming` or `isRunActive`, auto-queuing exactly ONE message. Enter key and Send button both route through `handleSend`'s guard. Inline amber banner shows "Still working on your last request" with auto-dismiss. Queued message auto-fires after in-flight completes.
- **Chat Bubble Truth Alignment (Part B):** STATUS_CONFIG labels changed: PARTIAL→"Results returned", STOP→"Search stopped". `buildSummaryText` now receives `towerVerdict`; when verdict is PARTIAL/STOP, avoids "match" language and uses "candidates, but could not verify the requirement." FAIL case gets explicit "Search could not be completed" text. Section headers use "Results" instead of "Matches" for STOP/PARTIAL. LeadBadge changed from "Verified match" to "Verified". UserResultsView "Closest matches" → "Closest results".
- **Clarify-Mode UX Enhancement:** Replaced the tiny amber "Clarifying before run" badge with a proper card that shows: (a) current interpreted request parsed from clarify messages (entity_type, location, semantic_constraint), (b) the pending question, (c) confirmation status when ready to search. Added `clarifyContext` state and `parseClarifyMessage()` to extract structured fields from backend messages without backend changes. Superseded clarify messages are hidden (`isClarifySuperseded` flag) to prevent duplicate rendering in T02-T03 scenarios. `streamIsClarifying` local variable ensures correct closure behavior in SSE handler. Clarify context is fully reset on new chat, load conversation, and session end.
- **Time Predicate Constraint Rendering (Batch 2):** UI now renders `ConstraintContract` from clarify_state payload. When `can_execute=false`, the clarify panel shows an explanation, proxy_options as clickable quick replies, and a "No proxy is acceptable (stop)" option. The user's selection is sent as a normal chat reply. `required_inputs_missing` from the constraint is rendered as a separate "Still needed" bullet list alongside constraint clarification. The thinking/typing indicator is suppressed during clarification-only states (`isClarifyingForRun`). RunResultBubble now shows "Time constraint handled via proxy: <proxy>" when Tower judgement includes `proxy_used`, and shows "Stopped: can't verify opening date constraint without an acceptable proxy" when Tower STOP is due to `time_predicate`. "Ready to search" and "Search now" prompts are suppressed when `can_execute=false`. Props: `towerProxyUsed`, `towerStopTimePredicate` flow from tower_judgement payload through Message type to RunResultBubble. Tests: `client/src/lib/__tests__/timePredicateUI.test.ts` (run via `npx tsx client/src/lib/__tests__/timePredicateUI.test.ts`).
- **Hard UI Block for can_execute=false (QA Fix):** Five execution-indicator leak paths closed: (1) "Searching for..." confidence bubble suppressed at SSE ingestion when `streamIsClarifying=true`. (2) Confidence bubble rendering returns null when `isClarifyingForRun && constraintContract.can_execute=false`. (3) Progress stack (executing/tool stages) hidden entirely during clarification via early-return guard. (4) Thinking indicator already guarded by `!isClarifyingForRun`. (5) "Search now" button guarded by `can_execute` check (previous pass). Constraint clarification panel now renders for ANY constraint type (not just `time_predicate`), with generic fallback explanation. Panel header changes to "Quick question" when `can_execute=false`, "Almost ready" otherwise. Compound clarification (e.g. time proxy + missing location + live music verification) renders all items in one panel. 20 tests cover all block paths.
- **Search Now Button Fix:** Fixed missing "Search now" CTA button after clarification resolves. Root cause: inline button checked for markdown `**Search now**` in message content, but markdown was stripped in copy cleanup. Fix: (1) Inline button uses case-insensitive regex `/search now/i` instead. (2) Added a state-driven "Search now" Button in the clarify status panel that renders based on clarify state (ready + no blocking constraints), independent of message content. Users always see a clickable CTA when clarification is resolved.
- **UI Copy Cleanup (Human-Tone Pass):** All user-facing clarification and confirmation language rewritten to sound conversational. Key changes: (1) Subjective gate says "I can't search by X directly" instead of "The term X is subjective and can't be measured". (2) Numeric ambiguity says "How many results would you like?" instead of "You said 'few' — how many results do you actually want?". (3) Relationship predicate says "may not be publicly verifiable" instead of long explanations. (4) Time predicate says "Exact opening dates aren't reliably available" instead of "Opening dates can't be guaranteed from public listings." (5) Confirmations use brief acks: "Got it — lively it is", "Okay, I'll use that as a proxy". (6) Questions use concise phrasing: "Where should I search?" instead of "Which location or area should I search in?". (7) UI labels: "Quick question" / "Almost ready" badges, "Looking for:" prefix, "What kind?" / "How many results?" / "Which approach?" section headers. (8) Single questions render without numbered list wrapper. (9) All markdown `**bold**` syntax removed from clarify session messages to prevent literal asterisks in chat bubbles. (10) `buildClarifyStatePayload` strips internal constraint labels (e.g. "numeric ambiguity: few") and numeric ambiguity words from entity type for clean UI display. (11) "top" and "most" removed from `NUMERIC_AMBIGUITY_WORDS` — these are ranking qualifiers, not quantity ambiguity, and should not trigger "how many?" prompts. Tests updated in `timePredicateUI.test.ts` and `numeric-ambiguity-gate.spec.ts`.

## External Dependencies
- **OpenAI GPT-5:** AI chat responses, prospect enrichment, web search, AI-generated personal lines.
- **Wyshbone Global Database (Google Places API):** Business discovery and location-based searches.
- **GeoNames API:** Worldwide administrative region discovery and geocoding.
- **Bubble:** External platform for backend workflows.
- **Resend API:** Transactional email notifications.
- **Xero:** Accounting platform integration.
- **Hunter.io:** Domain discovery, email finding, and verification.
- **SalesHandy:** Automated prospect management and campaign integration.

## Trust & Verification Safeguards (UI Layer)
- **Concatenation Guard (A):** `handleSend` in `chat.tsx` checks if a new outbound query contains the previous query as a substring (>10 chars). If detected, blocks send with an inline error: "Your request looks merged with the previous one. Please try again." Source of truth: `lastSentQueryRef` tracks the last query sent. State: `concatenationError`.
- **Pre-Run Sanity Gate (B):** Before sending a run request, `handleSend` calls `detectVerticalMismatch()` from `client/src/lib/verticalMismatch.ts`. If the active vertical (e.g. Breweries) doesn't match the user's query domain (e.g. "vulnerable adults"), shows a clarification bubble with two buttons: "Switch to General and run" / "Keep [Vertical]". Config: keyword-based mismatch heuristic per vertical stored in `MISMATCH_MAP`. Currently configured for `brewery` vertical only.
- **Tower Veto / Trust Failure (C):** `finalizeRunUI` extracts `towerVerdict` from `tower_judgement` artefacts. If verdict is FAIL/ERROR, the UI does NOT show "Results ready" toast. `RunResultBubble` renders a `TrustErrorBlock` with "Retry" / "Ask me a question" buttons instead of results. `deliveryStatus.ts` now includes `FAIL` as a canonical status.
- **Verified Match Badges (D):** `LeadBadge` in `RunResultBubble.tsx` now shows three states: "Verified match" (green, only when lead_verification confirms), "Candidate" (neutral gray, when verification exists but lead not confirmed), "Unverified" (gray with icon, when tower didn't judge or failed). `badgeStatus` prop propagated through `LeadRow`.
- **Debug Diagnostics (E):** `RunDiagnosticPanel` in `chat.tsx` now shows `lastSentQuery` (truncated to 60 chars) and `lastRunFinalVerdict` (PASS/FAIL/STOP/ERROR/UNKNOWN) in the client state section. Non-user-visible (dev only).