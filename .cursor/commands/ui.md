# yaml-language-server: $schema=https://raw.githubusercontent.com/cursor-ai/commands/main/command.schema.json

name: ui
description: Start the Wyshbone UI (frontend).

run: |
  cd "C:/Users/Phil Waite/Documents/GitHub/wyshbone-ui/client"
  npm run dev
