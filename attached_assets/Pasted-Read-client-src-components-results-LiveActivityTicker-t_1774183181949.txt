Read client/src/components/results/LiveActivityTicker.tsx. Make these THREE targeted fixes only. Do not rewrite the whole file.
FIX 1: The "Evidence verification complete" milestone appears too early
The verifyFilter function matches planning-phase events like "Verification Policy Selected" or "Artefact created: Verification attachment summary" because the condition s.includes('VERIFICATION') && (e.status === 'completed' || s.includes('COMPLETE')) is too broad.
Find the verifyFilter in deriveMilestones. Replace it with a much stricter filter that ONLY matches the actual end-of-verification event:
typescriptconst verifyFilter = (e: StreamEvent) => {
  const s = (e.summary || '').toUpperCase();
  const t = (e.details?.task || '').toUpperCase();
  // ONLY match the specific "Evidence verification: N/M checks passed" event
  // or "Final delivery: N leads" — NOT planning-phase verification artefacts
  return (s.includes('CHECKS PASSED') || t.includes('CHECKS PASSED')) ||
         (s.includes('EVIDENCE VERIFICATION:') || t.includes('EVIDENCE VERIFICATION:')) ||
         (s.includes('FINAL DELIVERY:') && (s.includes('LEADS') || t.includes('LEADS')));
};
This excludes "Verification Policy Selected", "Verification attachment summary", "Artefact created: Verification..." etc.
FIX 2: "Checking websites" milestone doesn't appear
The webVisitCompletedFilter requires s.startsWith('TOOL COMPLETED') which is too strict. Many web visit events have formats like "Executing Tool: WEB VISIT", "WEB VISIT: https://...", or "Artefact Created: WEB_VISIT:...".
Replace the webVisitCompletedFilter and the milestone 2 block. Find the section that starts with // ── Milestone 2: Checking websites. Replace the filter and the logic:
typescript// ── Milestone 2: Checking websites (only show AFTER GP search completes) ──
// Match ANY web visit related event (executing, completed, or artefact)
const webVisitAnyFilter = (e: StreamEvent) => {
  const s = (e.summary || '').toUpperCase();
  const t = (e.details?.task || '').toUpperCase();
  return (s.includes('WEB VISIT') || s.includes('WEB_VISIT') || t.includes('WEB VISIT') || t.includes('WEB_VISIT')) &&
         !s.includes('SEARCH_PLACES') && !s.includes('SEARCH PLACES');
};
const firstWebVisitEvent = findFirstAfter(webVisitAnyFilter, lastMilestoneTs);
if (firstWebVisitEvent) {
  // Count unique COMPLETED web visits by domain
  const webVisitCompletedFilter = (e: StreamEvent) => {
    const s = (e.summary || '').toUpperCase();
    return (s.startsWith('TOOL COMPLETED') || s.includes('TOOL COMPLETED')) && 
           (s.includes('WEB VISIT') || s.includes('WEB_VISIT'));
  };
  const completedMatches = sorted.filter(webVisitCompletedFilter);
  const seenDomains = new Set<string>();
  for (const e of completedMatches) {
    const task = e.details?.task || e.summary || '';
    const urlMatch = task.match(/https?:\/\/(?:www\.)?([^/\s&]+)/);
    if (urlMatch) seenDomains.add(urlMatch[1]);
    else seenDomains.add(e.id);
  }
  const completedCount = seenDomains.size;
  
  const gpMilestone = milestones.find(m => m.key === 'gp_search');
  const totalExpected = gpMilestone ? (extractCount(gpMilestone.text) ?? 0) : 0;
  
  const ts = new Date(firstWebVisitEvent.ts).getTime();
  let text: string;
  if (completedCount > 0 && totalExpected > 0) {
    text = `Checking websites — ${completedCount} of ${totalExpected} visited`;
  } else if (completedCount > 0) {
    text = `Checking websites — ${completedCount} visited so far`;
  } else {
    text = 'Checking pub websites for evidence...';
  }
  milestones.push({
    key: 'web_evidence',
    icon: '🌐',
    text,
    timestamp: ts,
  });
  lastMilestoneTs = ts;
}
FIX 3: Ephemeral line needs to show pub names and website domains
The ephemeral deriveEphemeral function is filtering out too many events and the name extraction isn't working. The issue is that pub names appear in the details.task field as part of strings like "Evidence: "Norfolk Tap" — website_evidence" or WEB_VISIT: https://norfolktap.com/ but the current patterns don't extract them well.
In the deriveEphemeral function, find the WEB VISIT handler. Replace it to also try extracting the entity name from the task text more aggressively:
Find this block:
typescriptif (summaryUp.includes('WEB VISIT') || summaryUp.includes('VISITING')) {
Replace the entire WEB VISIT if-block with:
typescriptif (summaryUp.includes('WEB VISIT') || summaryUp.includes('WEB_VISIT') || summaryUp.includes('VISITING')) {
  icon = '🌐';
  // Try multiple patterns to extract a meaningful name
  const nameMatch = (task || summary).match(/"([^"]+)"/);
  const urlMatch = (task || summary).match(/https?:\/\/(?:www\.)?([^/\s&]+)/);
  // Also try "WEB_VISIT: domain" or "WEB VISIT: domain" pattern
  const colonMatch = (task || summary).match(/(?:WEB.?VISIT|Visiting)[:\s]+(?:https?:\/\/(?:www\.)?)?([^/\s&"(]+)/i);
  if (nameMatch) {
    text = `Visiting ${nameMatch[1]}...`;
  } else if (urlMatch) {
    // Clean up the domain to be more readable
    const domain = urlMatch[1].replace(/\.co\.uk$|\.com$|\.org$|\.uk$/, '');
    text = `Visiting ${domain}...`;
  } else if (colonMatch) {
    text = `Visiting ${colonMatch[1].slice(0, 30)}...`;
  } else {
    text = 'Visiting website...';
  }
}
Also find the EVIDENCE handler and improve name extraction:
typescript} else if (summaryUp.includes('EVIDENCE') && !summaryUp.includes('VERIFICATION')) {
  icon = '📄';
  // Try to extract entity name from patterns like: Evidence: "Norfolk Tap" — ...
  const nameMatch = (task || summary).match(/"([^"]+)"/);
  // Also try: Checking Norfolk Tap... or Evidence: Norfolk Tap
  const colonNameMatch = (task || summary).match(/(?:Evidence|Checking)[:\s]+"?([^"—\n]+)"?/i);
  if (nameMatch) {
    text = `Found evidence for ${nameMatch[1]}`;
  } else if (colonNameMatch) {
    text = `Found evidence for ${colonNameMatch[1].trim().slice(0, 30)}`;
  } else {
    text = 'Checking evidence...';
  }
}
And add a new handler BEFORE the generic fallback to catch "Executing Tool: WEB VISIT" events which show the tool being actively run:
typescript} else if (summaryUp.includes('EXECUTING TOOL') || summaryUp.includes('EXECUTING:')) {
  icon = '🔧';
  // Extract the tool name and make it human-readable
  const toolMatch = (task || summary).match(/(?:Executing(?:\s+Tool)?)[:\s]+(.+)/i);
  if (toolMatch) {
    const tool = toolMatch[1].trim();
    if (/WEB.?VISIT/i.test(tool)) {
      const urlMatch = tool.match(/https?:\/\/(?:www\.)?([^/\s&]+)/);
      text = urlMatch ? `Visiting ${urlMatch[1]}...` : 'Visiting website...';
      icon = '🌐';
    } else {
      text = `Running ${tool.slice(0, 35)}...`;
    }
  } else {
    text = 'Executing...';
  }
}
Insert this new handler AFTER the EXCLUSION FILTER handler and BEFORE the SEARCH_PLACES skip.
Do NOT change anything else — the milestone gating logic, the rendering JSX, the ThinkingBrains component, and the polling logic are all correct.
Work on the main branch only — do not create or switch branches. After completing this task, append a structured summary to AGENT_LOG.md (what changed, files modified, decisions made, what's next).