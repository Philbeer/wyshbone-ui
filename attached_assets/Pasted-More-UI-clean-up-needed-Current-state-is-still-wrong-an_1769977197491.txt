More UI clean-up needed. Current state is still wrong and cluttered.

1) Still not showing “Completed” in the UI
The run finishes (I can see it in AFR) but Live Activity does not reliably switch to Completed / Sequence complete.
Please fix terminal display so it ALWAYS flips when the run is actually finished.

Non-negotiable:
- If AFR says terminal for the active request, Live Activity must show Completed + stop the working animation.

2) Remove the extra “Supervisor made a plan” / plan panel UI
A “Supervisor created a plan” panel with Stop/Regenerate etc popped up. We do NOT want that.
We are not using plan approval or plan management UI anymore.

Requirement:
- Remove the Plan panel content entirely (the “Supervisor created a plan”, stop/regenerate, etc).
- We only want one place to see what’s happening: the Live Activity tree.

3) Only ONE Stop button, inside Live Activity
We want a single Stop button in the Live Activity panel header (or next to the “Executing/Working” badge).
- No Stop buttons anywhere else.
- When clicked, it stops the current active run (whatever endpoint you wired).
- Disable/hide Stop when there is no active run.

4) Remove the Progress panel
Now that Live Activity is the main real-time tracker, the Progress panel is redundant and is in the wrong place.
Requirement:
- Remove the Progress panel completely.
- The right column should just be:
  - Live Activity (tall, scrollable)
  - (Optionally) Plan-for-goal card can remain if you still need it, but no approval UI.

5) Layout fix: Live Activity height vs right column spacing
Live Activity got taller (good) but now everything else is jammed.
Requirement:
- Make the right sidebar a clean vertical stack.
- Live Activity takes ~70% height and scrolls internally.
- Any remaining cards sit below it with proper spacing.
- No floating/overlapping or “pushed up” weirdness.

Acceptance criteria:
- Run completes -> Live Activity flips to Completed and shows Sequence complete.
- There is exactly ONE Stop button, and it is inside Live Activity only.
- No “Supervisor created a plan” panel anywhere.
- Progress panel is removed.
- Right sidebar layout looks clean: Live Activity dominates, other content below.

Please implement and share a screenshot of the right sidebar showing:
- Live Activity with correct completed state
- Single Stop button
- No plan panel
- No progress panel
