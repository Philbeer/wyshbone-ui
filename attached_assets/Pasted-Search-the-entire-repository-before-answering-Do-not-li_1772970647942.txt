Search the entire repository before answering. Do not limit to open files.

I want a production-grade UI change in the AFR / Agent Flight Recorder screen.

Goal:
Add a bulk export control next to the existing “Export run” / “Explain last run” area so I can export the most recent N runs for debugging.

Requirements

1. Add a numeric input box beside the export control.
   - Label it clearly, e.g. “Export last”
   - Default value: 5
   - Minimum: 1
   - Maximum: 15
   - Prevent values above 15 in the UI
   - If user types a higher number, clamp it to 15

2. Add a new button next to the existing export button.
   - Button text: “Export last N runs” or similar clear wording
   - It should use the numeric input value
   - Example: if input = 5, export the latest 5 runs

3. Export behaviour
   - Export the most recent runs from the current AFR run list / current filter context
   - Respect the current ordering shown in the UI
   - Only export up to the allowed maximum of 15 runs
   - If fewer than N runs exist, export what is available
   - Format should match the existing single-run export style as closely as possible
   - Prefer one combined export file rather than multiple separate downloads unless the current architecture strongly prefers separate files

4. UX requirements
   - Place the new input and button neatly next to the existing export controls in the right-hand panel/header area shown in AFR
   - Keep styling consistent with the current design system
   - Show disabled/loading state while exporting
   - Do not break the existing single-run export
   - Keep this clearly debug/admin oriented, not consumer-facing

5. Safety / robustness
   - Hard cap at 15 both in UI and in the export handler logic
   - Handle empty run list gracefully
   - Handle missing run data gracefully
   - No hacky timeout-based behaviour

6. Implementation expectations
   - Reuse existing export logic where sensible
   - Do not duplicate export formatting code if a shared helper can be extracted cleanly
   - Keep this change isolated to the AFR / debugging surface
   - Make the solution production-grade, not a quick patch

What I want back from you

1. The exact files you changed
2. A short explanation of the approach
3. Any new helper functions/components added
4. Confirmation of how the max-15 safety cap is enforced