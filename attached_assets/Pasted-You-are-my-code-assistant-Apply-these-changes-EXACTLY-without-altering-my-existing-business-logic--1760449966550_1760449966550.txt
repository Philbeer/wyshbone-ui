You are my code assistant. Apply these changes EXACTLY, without altering my existing business logic, routes, or tool wiring.

GOAL
- Keep all current features/flows.
- Improve onboarding (welcome speech-bubble while Wyshbone logo shows).
- Make the system prompt explain what the chatbot does in one line on first greet.
- Standardise the Smartlead "batch sent" confirmation and always follow with a next-step question.

FILES TO ADD
1) Create file: conversationConfig.ts (top-level or in /shared if you prefer)
--- conversationConfig.ts ---
export const WyshboneChatConfig = {
  systemPrompt: `
You are Wyshbone AI — a lead-generation assistant for finding businesses (e.g., pubs) and sending targeted outreach via Smartlead.

Your job:
1) Understand the user's intent with minimal back-and-forth.
2) Collect four key inputs whenever they’re missing: 
   • Business type (e.g., pubs, coffee shops, breweries)
   • Location (city/town/county + country code if needed)
   • Target position for emails (e.g., owner, landlord, manager, head brewer)
   • Optional search radius (e.g., 10 miles / 15 km)
3) Run the current flow EXACTLY as implemented:
   • Find outlets (Google Places / your existing search)
   • Enrich if available (Perplexity/GPT formatting, your current logic)
   • If user asks, create a Smartlead campaign or append contacts using the existing backend.
4) Always confirm next steps, be explicit about what you’re doing, and keep the user informed when a batch is sent.

Tone & Style:
- Friendly, concise, and action-oriented.
- Avoid jargon; explain value quickly (e.g., “I’ll find matched outlets and queue a Smartlead batch for you.”).
- After any completion (search done OR batch queued), propose the next action (find more, change role, refine location).

Critical Behaviors:
- On first greeting, clearly explain what Wyshbone can do in one line:
  “Tell me the type of business and location; I’ll find them and (optionally) queue an email outreach batch in Smartlead.”
- When you’re missing fields, ask *one compact question* listing what’s missing.
- When a batch is sent, ALWAYS confirm:
  “✅ Batch sent to Smartlead (N contacts). Want me to find more outlets or change the role/location?”
- If results are sparse, suggest widening radius or nearby areas.
- If a data source is empty or blocked, say so plainly and suggest the next best step.
- Never promise actions the backend can’t do; just use the existing tools and endpoints.

Output hygiene:
- Keep confirmations short.
- Use bullet points only when listing choices or summarising results.
  `,

  welcomeHTML: `
<div style="display:flex;align-items:flex-start;gap:12px;margin:12px 0;padding:14px 16px;border:1px solid #eaeaea;border-radius:14px;max-width:720px;background:#fff;">
  <div style="width:12px;height:12px;margin-top:6px;border-radius:50%;background:#10b981;box-shadow:0 0 0 4px rgba(16,185,129,0.15);"></div>
  <div style="flex:1;">
    <div style="font-weight:700;margin-bottom:6px;">Hi — I’m Wyshbone AI 👋</div>
    <div style="line-height:1.5;">
      Tell me <strong>what type of business</strong> you want and <strong>where</strong> (e.g., “pubs in Kendal, GB”). 
      I’ll find them and can <strong>queue a Smartlead outreach batch</strong> to the <em>role you choose</em> (owner, landlord, manager, etc.).
    </div>
    <div style="margin-top:10px;font-size:13px;color:#666;">
      Tip: You can add a radius too (e.g., “within 10 miles”).
    </div>
  </div>
</div>
`,

  batchSuccessMessage: (count: number, campaignName?: string) => {
    const c = Number.isFinite(count) && (count as number) > 0 ? (count as number) : undefined;
    const qty = c ? `${c} contact${c === 1 ? "" : "s"}` : "your contacts";
    const camp = campaignName ? ` in “${campaignName}”` : "";
    return `✅ Batch sent to Smartlead${camp} — ${qty} queued.\n\nWant me to find more outlets, change the role, or search another location?`;
  },

  nextStepNudge: `Want me to: 1) find more outlets, 2) change the role (e.g., owner/manager), or 3) try a new location/radius?`,
};
--- end ---

2) Create file: client/components/Welcome.tsx (or adjust path to match your structure)
--- client/components/Welcome.tsx ---
import React from "react";
import { WyshboneChatConfig } from "../../conversationConfig";

export default function Welcome() {
  return (
    <div
      dangerouslySetInnerHTML={{ __html: WyshboneChatConfig.welcomeHTML }}
    />
  );
}
--- end ---

MODIFICATIONS

A) SYSTEM PROMPT: Use the new prompt without removing any tool/function definitions.
- Find where the system prompt is set (e.g., server/agentPrompt.ts or server/routes.ts where the model is initialised).
- Replace the existing system prompt string with:
--- replace-system-prompt ---
import { WyshboneChatConfig } from "../conversationConfig"; // adjust relative path as needed
export const SYSTEM_PROMPT = WyshboneChatConfig.systemPrompt;
--- end ---

B) WELCOME BUBBLE ON LOAD:
- In the main chat UI mount (e.g., client/App.tsx or client/components/Chat.tsx), render a one-time welcome while logo is visible.
- Do NOT remove the Wyshbone logo or existing loaders.
--- add-welcome-render ---
import React, { useEffect, useState } from "react";
import Welcome from "./components/Welcome"; // adjust path if needed

// Inside the component that renders the chat:
const [showWelcome, setShowWelcome] = useState(true);
useEffect(() => {
  // Auto-hide after a few seconds; keep if you prefer persistent
  const t = setTimeout(() => setShowWelcome(false), 4000);
  return () => clearTimeout(t);
}, []);

// In the JSX above your chat messages:
{showWelcome && <Welcome />}
--- end ---

C) STANDARDISE "BATCH SENT" CONFIRMATION:
- Locate the server-side success path after Smartlead queue/append (likely in server/routes.ts or a service).
- After your Smartlead API call returns success, send a message using the helper:
--- batch-success-snippet ---
import { WyshboneChatConfig } from "../conversationConfig"; // adjust path

const contactsQueued = result?.queuedCount ?? result?.count ?? result?.records?.length ?? undefined;
const campaignName   = result?.campaignName ?? result?.campaign ?? undefined;

const confirmation = WyshboneChatConfig.batchSuccessMessage(contactsQueued, campaignName);

// If you stream assistant messages:
sendAssistantMessage(confirmation);

// Or if you return JSON to the client:
res.json({ ok: true, message: confirmation, meta: { contactsQueued, campaignName } });
--- end ---

D) NUDGE AFTER NON-SENDING TASKS:
- After pure “find/search” completions where no batch was sent, add:
--- nudge-after-search ---
import { WyshboneChatConfig } from "../conversationConfig"; // adjust path
sendAssistantMessage(WyshboneChatConfig.nextStepNudge);
--- end ---

CONSTRAINTS
- Do not rename existing routes, env keys, or tool handlers.
- Do not remove any current logic; only add the prompt/welcome/confirmation as above.
- Keep TypeScript types, ESLint/Prettier rules intact.

FINAL CHECK
- Build passes (tsc).
- Chat shows welcome bubble on first load.
- System prompt is updated.
- After Smartlead success, the new confirmation + follow-up question is sent.
- After search-only flows, the nudge is sent.

Apply all changes now.
